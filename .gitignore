node_modules/
dist/
.DS_Store
*.log
.vite/
coverage/
__pycache__/

# Per-season CSV folders under pipeline/data/raw/seasons/ ARE tracked (they
# fully reproduce the site data). The raw Transfermarkt/Kaggle dumps are large
# and only needed locally to (re)generate those folders, so they're ignored.
pipeline/data/raw/transfermarkt/*.csv
